node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env.*.local
.env.test
playwright-report/
test-results/
tmp/
tmp-smoke/
.tmp-smoke/
.gstack/
.env*.local
.claude/
.venv-fonttools/
coverage/

# Vercel CLI link metadata — contains projectId/orgId. Per Vercel's own
# README.txt: "you should not share the '.vercel' folder with anyone."
.vercel/

# Credentials / certs — should never be tracked.
*.pem
*.key
*.p12

# Clawpatch review tooling
.clawpatch/

# Stray local-tool logs (Codex CLI, debugger dumps, npm-debug, etc.).
*.log

# OS clutter
.DS_Store
Thumbs.db
.vercel

# also ignore a bare node_modules file/symlink (the trailing-slash rule above only matches the dir)
node_modules
.env.production

# audit-sweep dev scripts (never commit)
patch_batch*.py

# Astro marketing (Phase 1 merge)
marketing/node_modules/
marketing/dist/
marketing/.astro/
marketing/.vercel/

# Astro marketing generated output (built into public/ by vercel buildCommand)
/public/index.html
/public/_astro/
/public/es/
/public/fr/
/public/pt/
/public/about/
/public/what-we-do/
/public/how-it-works/
/public/faq/
/public/contact/
/public/impact/
/public/outcomes/
/public/donate/
/public/privacy/
/public/terms/
/public/accessibility/
/public/programs/
/public/program-comparison/
/public/salary-guide/
/public/lp/
/public/blog/
/public/leadership/
/public/employers/
/public/partners/
/public/careers/
/public/mentor/
/public/find-your-path/
/public/career-quiz/
/public/interest-profiler/
/public/apply/
/public/m/

# one-off debug/scratch artifacts accidentally committed; keep out for good
/cursor-api-payload.json
/git_script.sh
/next-build-output-verify.txt

# CI runner has intermittently hung on the Build step this session with
# no completion signal; these blank-line commits force a fresh runner.



















# Local screenshot output (scripts/portal-screenshots.mjs)
.artifacts/

# Python cache (ui-ux-pro-max skill scripts)
__pycache__/

# Merge/patch artifacts (never commit these)
*.orig
*.patch

# Generated knowledge-base index. Build output, not source: run `npm run kb:generate`
# to produce it locally; CI regenerates and verifies it on every pull request.
# It was tracked until it became the dominant source of merge conflicts between PRs.
docs/knowledge-base/generated/
