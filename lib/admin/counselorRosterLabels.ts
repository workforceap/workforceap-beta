/**
 * Captions printed beside the /admin/counselors KPI tiles so the rule behind
 * each number is visible on the page (number audit 2026-09-20, S27). Pure
 * strings: shared by the server-side aggregates and the client kit.
 */
export const COUNSELOR_AT_RISK_DEFINITION = 'assigned members in a program with an open, acknowledged or escalated saved risk alert';
export const COUNSELOR_PLACEMENTS_DEFINITION = 'assigned members with a placement record';
